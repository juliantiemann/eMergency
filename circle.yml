dependencies:
  pre:
    - bower install; gem install compass;
deployment:
  live:
    branch: master
    commands:
      - grunt deploy;

test:
  post:
    - mkdir -p ./test/junit/
    - find . -type f -regex "./test/out/junit/*/.*xml" -exec cp {} ./tests/junit/ \;
